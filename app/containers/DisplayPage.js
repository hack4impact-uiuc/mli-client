import React, { Component } from 'react';
import Display from '../components/Display';

type Props = {};

export default class DisplayPage extends Component<Props> {
  props: Props;

  render() {
    return <Display />;
  }
}
